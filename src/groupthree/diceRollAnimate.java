package groupthree;

import javafx.animation.KeyFrame;
import javafx.animation.KeyValue;
import javafx.animation.Timeline;
import javafx.util.Duration;

import java.awt.*;

public class diceRollAnimate extends FarkleController {

    public void cycleImages() {
        /*
        final Timeline diceImages = new Timeline(
               Image numOne = new Image();
              Image numTwo = new Image("");
                 new KeyFrame(Duration.ZERO, new KeyValue(rect0.fillProperty(), new ImagePattern() )),
                new KeyFrame(Duration.seconds(3), new KeyValue(imageView.imageProperty(), image2)),
                new KeyFrame(Duration.seconds(8), new KeyValue(imageView.imageProperty(), null))
        );
*/

    }

}
